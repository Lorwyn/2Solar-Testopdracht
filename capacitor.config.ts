import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '2Solar Testopdracht',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
